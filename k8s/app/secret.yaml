# Não commitar valores reais aqui. Este manifesto é só um placeholder para o
# GitOps saber que o recurso existe — o valor de fato é aplicado fora do git,
# via `kubectl create secret generic oficina-db-app --from-literal=username=... --from-literal=password=...`
# (localmente, na pipeline de CI/CD com um secret do GitHub Actions, ou via
# Azure Key Vault / CNPG, dependendo do ambiente).
apiVersion: v1
kind: Secret
metadata:
  name: oficina-db-app
type: Opaque
stringData:
  username: CHANGE_ME
  password: CHANGE_ME
